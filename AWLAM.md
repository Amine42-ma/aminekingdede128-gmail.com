# عوالم · AWLAM — منصّة ومحرّك ألعاب ثلاثي الأبعاد في ملف HTML واحد

> ملف واحد: **`awlam.html`** — افتحه ليعمل الموقع كاملاً: صفحة ألعاب، محرّك ألعاب، مكتبة نماذج عامة، نشر، ولعب.

منصّة تشبه فكرة روبلوكس لكن بتصميم مختلف: أي شخص يدخل، يصنع لعبته ثلاثية الأبعاد بالسحب والإفلات،
يُلصق عليها الميكانيكيات الجاهزة (حركة لاعب، سيارات، ذكاء اصطناعي للأعداء، نقاط، أبواب، بوابات نقل…)،
أو يكتب كوداً — أو يدع الذكاء الاصطناعي يكتبه له — ثم ينشرها فيلعبها الجميع.

---

## التشغيل

```bash
# من داخل مجلد المستودع
python3 -m http.server 8000
# ثم افتح: http://localhost:8000/awlam.html
```

أو ارفع الملف على أي استضافة ثابتة (GitHub Pages / Netlify / Vercel) وافتح رابطه.

* يحتاج **اتصالاً بالإنترنت** في أول تحميل ليجلب محرّك الرسوميات `three.js` من CDN وخطوط Google.
* المتصفحات المدعومة: Chrome / Edge / Firefox / Safari حديثة، ويعمل على الجوال بأزرار لمس.
* فتح الملف مباشرة بنقرة مزدوجة (`file://`) يعمل أيضاً في أغلب المتصفحات، لكن الخادم المحلي أضمن.

---

## ربط Firebase (لجعل كل شيء مشتركاً بين الناس)

الإعدادات مضمّنة داخل الملف مسبقاً لمشروع `jknbb-n`. يبقى شيئان في لوحة تحكّم Firebase:

### 1) قواعد Realtime Database

من Firebase Console → **Realtime Database → Rules** ضع:

```json
{
  "rules": {
    "games":    { ".read": true, ".write": true },
    "gameData": { ".read": true, ".write": true },
    "assets":   { ".read": true, ".write": true },
    "blobs":    { ".read": true, ".write": true },
    "likes":    { ".read": true, ".write": true },
    "players":  { ".read": true, ".write": true },
    "users":    { ".read": true, ".write": true },
    "presence": { ".read": true, ".write": true }
  }
}
```

⚠️ **تنبيه صريح:** هذه قواعد مفتوحة للجميع (أي شخص يملك الرابط يستطيع القراءة والكتابة والحذف).
هي المناسبة لمنصّة تجريبية يشارك فيها الجميع، لكنها ليست آمنة لبيانات حسّاسة.
لاحقاً يمكن تضييقها بربط الكتابة بالمستخدم المسجَّل (`auth != null`) أو بتفعيل App Check.

### 2) Storage (لملفات النماذج والموسيقى الكبيرة)

من **Build → Storage** فعّل التخزين، ثم في **Rules**:

```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /assets/{file} {
      allow read: if true;
      allow write: if request.resource.size < 48 * 1024 * 1024;
    }
  }
}
```

ومن **Authentication → Sign-in method** فعّل **Anonymous** (يحتاجه التخزين عادةً).

### إن لم تُضبط القواعد

التطبيق **لا يتعطّل**: يتحوّل تلقائياً إلى **الوضع المحلي** (كل شيء يُحفظ في متصفحك)،
وتظهر النقطة بجوار اسمك رمادية بدل الخضراء مع رسالة توضّح السبب.
كما أنّ الملفات الأصغر من 14 م.ب تُحفظ داخل قاعدة البيانات نفسها إن كان Storage غير مفعّل.

---

## ما الذي يحتويه؟

### 1. الموقع
* واجهة عربية كاملة (RTL) بتصميم داكن احترافي.
* شبكة الألعاب مع أيقونة، غلاف، اسم، صاحب اللعبة، عدد مرّات اللعب والإعجابات.
* فرز: الأكثر رواجاً / الأحدث / الأكثر إعجاباً + 8 تصنيفات + بحث.
* صفحة لكل لعبة: وصف، إحصائيات، **قائمة من جرّب اللعبة**، إعجاب، مشاركة رابط، «نسخة لي»، وتصدير.
* «أعمالي»: ألعابك وملفاتك المرفوعة مع تحرير/تصدير/حذف.

### 2. المحرّك (الاستوديو)
* مشهد ثلاثي الأبعاد حقيقي: ظلال، ضباب، سماء متدرّجة، نجوم ليلاً، إضاءة شمس قابلة للتحريك.
* **4 أنماط بصرية** تُبدَّل بنقرة: واقعي · كرتوني (Toon) · ليلي نيون · غروب.
* أدوات: تحريك / تدوير / تحجيم بمقبض ثلاثي الأبعاد، محاذاة للشبكة، تكرار، حذف، تراجع/إعادة، تركيز.
* لوحات: **بناء** (المجسمات الجاهزة) · **مكتبتي** (ملفاتك والمكتبة العامة) · **الخصائص** · **الهيكل** · **العالم**.
* سحب وإفلات أي عنصر من اللوحة إلى المشهد مباشرة.
* حفظ تلقائي كل 25 ثانية + حفظ يدوي (Ctrl+S).

### 3. المجسمات الجاهزة (أكثر من 50)
| الفئة | أمثلة |
|---|---|
| أشكال | مكعب، كرة، أسطوانة، مخروط، كبسولة، حلقة، منحدر، سطح |
| أرضيات | أرض عشبية، منصّة، طريق، جسر، درج، ماء |
| مبانٍ | منزل، برج، قلعة، ناطحة سحاب، متجر، جدار، سياج، باب، صندوق، برميل |
| طبيعة | شجرة، صنوبر، صخرة، شجيرة، زهرة، جبل، سحابة |
| مركبات | سيارة، دراجة نارية، شاحنة، قارب (كلها قابلة للقيادة + راديو) |
| شخصيات | شخصية، عدو، كلب، قطة، حصان، طائر، تنين (بحركة مفاصل حقيقية) |
| عناصر لعب | نقطة بداية، عملة، جوهرة، قلب، مفتاح، نقطة حفظ، خط نهاية، منصّة قفز، حمم، أشواك، بوابة نقل، زر، لافتة |
| مؤثرات | عمود إنارة، ضوء ملوّن، نار، جزيئات، مصدر صوت |

### 4. الميكانيكيات الجاهزة (23 ميكانيكية بنقرة واحدة)
دوران · طفو · حركة ذهاب وإياب · دوران حول نقطة · نبض · تغيّر الألوان · قابل للجمع · منطقة خطر ·
منطقة شفاء · منصّة قفز · بوابة نقل · نقطة حفظ · خط نهاية (فوز) · باب (بالاقتراب/بمفتاح/بزر) ·
زر تشغيل · لافتة نصية · ماء وسباحة · صوت وموسيقى · مولّد مجسمات · قابل للكسر ·
**ذكاء اصطناعي** (ثابت/تجوال/دورية/يتبع/يطارد ويهاجم/يهرب/يطير) · **مركبة قابلة للقيادة** · كود مخصّص.

لكل ميكانيكية إعداداتها الخاصة داخل لوحة الخصائص (سرعة، ضرر، مدى الرؤية، نقاط، أصوات…).

### 5. الأكواد + الذكاء الاصطناعي
* محرّر أكواد مدمج مع مقتطفات جاهزة.
* زر 🤖 **«اكتب لي الكود»**: تكتب بالعربية «اجعله يطارد اللاعب وعندما يلمسه يعطيه 30 نقطة» فيُكتب الكود.
  * يعمل **بدون إنترنت ومجاناً** عبر محرّك النوايا المدمج (أكثر من 25 نمطاً عربياً/إنجليزياً).
  * أو اربط مفتاح **Gemini** أو **OpenAI** من ⚙️ للحصول على أكواد أعقد (المفتاح يُحفظ على جهازك فقط).

واجهة البرمجة المتاحة داخل الكود:

```js
on('start', fn) · on('update', dt => {}) · on('touch', who => {}) · on('key', 'e', fn) · every(ثوانٍ, fn)

self.rotate(x,y,z) · self.move(dx,dy,dz) · self.setPos() · self.setColor() · self.setScale()
self.hide() · self.show() · self.destroy() · self.lookAtPlayer() · self.distanceToPlayer()
self.moveForward(n) · self.burst() · self.playSound(id)

player.pos · player.hp · player.score · player.addScore(n) · player.damage(n) · player.heal(n)
player.teleport(x,y,z) · player.jump(f) · player.push(x,y,z) · player.hasKey(k) · player.onGround

world.spawn('coin', x,y,z) · world.find('اسم') · world.msg('نص') · world.win('نص') · world.playMusic(id)

rand(a,b) · dist(a,b) · lerp(a,b,t) · clamp(v,a,b) · time() · keyDown('w') · vec(x,y,z)
```

### 6. المكتبة العامة (ملفات الجميع)
* ارفع **GLB / GLTF** (نماذج)، **MP3 / WAV / OGG** (موسيقى وأصوات)، **PNG / JPG** (خامات)، وأي ملف آخر.
* أي ملف تضع علامة «مشاركته مع الجميع» عليه **يصبح متاحاً لكل الناس** داخل المحرّك فوراً.
* النماذج تُضاف للمشهد بنقرة، الصور تُطبَّق كخامة على المجسم المحدّد، والأصوات تصبح مصادر صوت أو مقاطع راديو.
* ملفات PDF غير مدعومة، والحد الأقصى 48 م.ب للملف.

### 7. اللعب
* شخص ثالث أو أول (مفتاح `V`)، مشي وركض وقفز، صحة، نقاط، أهداف، نقاط حفظ، فوز وخسارة.
* **المركبات**: اقترب واضغط `E` للركوب، `W/S` تسارع، `A/D` توجيه، ومعها **راديو** يشغّل أي موسيقى مرفوعة.
* دعم الجوال: عصا تحكّم وأزرار لمس تلقائية.
* عدّاد مرّات اللعب وقائمة اللاعبين وحفظ الإعجابات في السحابة.

### 8. التصدير
زر «تصدير اللعبة» ينزّل **ملف HTML واحد مستقلّ** يفتح اللعبة مباشرة على أي جهاز بدون الموقع.

---

## اختصارات لوحة المفاتيح

| في المحرّك | | في اللعب | |
|---|---|---|---|
| `W` | تحريك | `W A S D` | حركة |
| `E` | تدوير | `Space` | قفز |
| `R` | تحجيم | `Shift` | ركض |
| `F` | تركيز على المحدد | `E` | تفاعل / ركوب |
| `Ctrl+D` | تكرار | `V` | تبديل المنظور |
| `Delete` | حذف | `Esc` | إيقاف مؤقت |
| `Ctrl+Z` / `Ctrl+Y` | تراجع / إعادة | | |
| `Ctrl+S` | حفظ | | |

---

## بنية البيانات في Firebase

```
games/<gameId>      → { name, desc, genre, icon, cover, author, authorId, plays, likes, objs, style, pub }
gameData/<gameId>   → نص JSON للمشهد كاملاً { env, set, objs[] }
assets/<assetId>    → { name, type, mime, size, store, url, by, byName, at }
blobs/<assetId>     → أجزاء base64 للملفات عند عدم تفعيل Storage
likes/<gameId>/<uid>
players/<gameId>/<uid> → { n, c, t }   ← من جرّب اللعبة
users/<uid>         → { name, color, seen }
```

## ملاحظات تقنية

* الفيزياء مكتوبة داخل الملف (جاذبية، اصطدام صناديق مائلة، صعود الدرجات، منصّات متحرّكة، سباحة) — بلا مكتبات خارجية.
* الرسوميات: `three.js r160` من CDN عبر importmap فقط؛ كل شيء آخر مضمّن في الملف.
* الأيقونة والغلاف تُصغَّران داخل المتصفح قبل الرفع لتوفير المساحة، ويمكن التقاط الغلاف من المحرّك مباشرة.
* جودة الرسوميات (عالية/متوسطة/منخفضة) قابلة للتغيير من ⚙️ لتناسب الأجهزة الضعيفة.
