# GameMorph.AI — مهندس ألعاب HTML5

استوديو من **ملف HTML واحد** يفكّك أكواد ألعاب Canvas ويستخرج ميكانيكياتها، ويولّد ألعاباً كاملة
تُصدَّر كملف واحد مستقل يعمل على الهاتف بأزرار لمس ومؤثرات 8-bit وجزيئات — **بلا إنترنت وبلا خادم**.
المحرّك يعمل داخل متصفحك؛ لا يُرفع أي كود ولا أي ملف إلى أي مكان.

---

## كيف يعمل

```
فكرة أو كود ──▶ الطرفية التفاعلية ──▶ تفكيك واستخراج ──▶ مواصفات المشروع ──▶ ملف HTML مستقل
```

الطرفية تفهم العربية الطبيعية وتترجمها إلى تعديل فعلي على اللعبة، ثم تُعاد المعاينة فوراً داخل محاكي هاتف.

## طريقة التشغيل

افتح `gamemorph.html` بنقرة مزدوجة — لا يحتاج خادماً ولا تثبيتاً ولا نماذج تُنزَّل.
وإن فضّلت خادماً محلياً:

```bash
python3 -m http.server 8000     # ثم: http://localhost:8000/gamemorph.html
```

**المتصفح:** Chrome أو Edge أو Safari حديث. الخطوط تأتي من الإنترنت إن توفّر، وبدونه يعمل كل شيء بخطوط النظام.

## الأربع قدرات

### ١ · الطرفية — تتحدّث فتُنفَّذ

اكتب بلغتك الطبيعية، والمحرّك المحلي يحوّل الجملة إلى تعديل حقيقي ويعيد بناء اللعبة:

| تكتب | يحدث |
|---|---|
| `اصنع لعبة ثعبان بلوحة غابة` | يبدّل النوع واللوحة والأسماء والنصوص |
| `زد الجاذبية ٣٠٪` | حساب نسبي على القيمة الحالية مع شرح أثره على القفزة |
| `اجعل الأرواح ٥` · `أضف قفزة مزدوجة` · `بدون موسيقى` | ضبط مباشر للقواعد والمؤثرات |
| `سمِّ البطل زيد` | يغيّر اسم البطل ونصوص الشاشات معه |
| `اشرح coyote time` · `اعطني كود التصادم AABB` | شرح + كود جاهز للنسخ من قاعدة معرفة داخلية |
| `ابحث عن أحدث مكتبات Canvas` | بحث حيّ (ويكيبيديا وDuckDuckGo بلا مفتاح، أو Gemini إن ربطته) |
| `صدّر اللعبة` | ينزّل الملف المستقل |

### ٢ · المحلل — يفكّك أي كود لعبة

أسقِط ملف `.html` أو `.js` في تبويب **المحلل** فيستخرج:

- **الفيزياء**: الجاذبية، قوة القفز، السرعة، الاحتكاك، الأرواح — بأرقامها الفعلية من الكود.
- **البنية**: حلقة المعالجة، زمن الإطار، كشف التصادم، نظام النقاط، آلة الحالات، الأصناف والدوال.
- **الإدخال والعرض**: لوحة مفاتيح، لمس، تجاوب مع الشاشة، وسم viewport، صوت، جزيئات، حفظ التقدّم.
- **النوع المرجّح** للعبة، ودرجة اكتمال من ١٠٠ مقارنةً بمعايير لعبة HTML5 حديثة.

ثم بضغطة واحدة: **استيراد الفيزياء** إلى مشروعك (مع تحويل قيم «لكل إطار» إلى «لكل ثانية» تلقائياً:
الجاذبية ×٣٦٠٠ والسرعات ×٦٠)، أو **تحويلها إلى نسخة موبايل كاملة** بأزرار لمس وصوت وجزيئات.

### ٣ · التدريب — يتعلّم أسلوبك وقصتك

أسقِط ملف `.txt` أو `.md` (أو الصق النص) في تبويب **التدريب**، فيُعامَل الملف كتوجيه مباشر:

- **النبرة**: رسمية أو ودّية، حماسية أو هادئة، مختصرة أو مفصّلة — وإن ذكرت أسلوبك صراحةً («أسلوبك حماسي ومختصر») قُدِّم على التحليل الإحصائي.
- **القواعد**: كل سطر توجيهي يُستخرج ويُلتزم به في الحوار.
- **المطلوب**: النوع، أسماء البطل والعدو والمجموعات، الجاذبية والقفز والصعوبة، لوحة الألوان، وسطر البداية — كلها تُطبَّق على اللعبة فوراً.

### ٤ · التصدير — ملف واحد يعمل وحده

زر التنزيل يعطيك ملف HTML واحد (~٦٢ KB) بداخله كل شيء: المحرّك، الأصوات، الجزيئات، الأزرار.
لا صور، لا ملفات صوتية، لا مكتبات، **ولا طلب شبكة واحد**.

## الأنواع الستة

| النوع | التحكّم | الفكرة |
|---|---|---|
| **منصّات** | يمين/يسار + قفز | منصات وفجوات وأعداء وعملات وراية نهاية |
| **عدّاء** | قفز + انزلاق | جري لا نهائي، عقبات تُقفز وحواجز يُنزلق تحتها |
| **رفرفة** | نقرة في أي مكان | عبور فجوات بين الأنابيب |
| **إطلاق** | ٤ اتجاهات + نار | موجات أعداء وصخور ورصاص مضاد |
| **كسر طوب** | سحب الإصبع | مضرب وكرة وطبقات طوب متعددة الصلابة |
| **ثعبان** | سحب أو أسهم | شبكة ونمو وسرعة متزايدة |

جميعها تعمل باللمس وبلوحة المفاتيح معاً، وفي الوضعين العمودي والأفقي.

## ما يضمنه المولِّد

- **مرحلة قابلة للعبور دائماً**: المراحل تُولَّد من فيزياء اللاعب نفسها — لا فجوة أوسع من مدى قفزته،
  ولا حافة أعلى من ارتفاع قفزته، ولا منصة معلّقة خارج متناوله. غيّر الجاذبية إلى أي قيمة وستبقى المرحلة قابلة للإنهاء.
- **مساحة لعب نظيفة**: لا شيء قابل للّعب يُرسم تحت أزرار اللمس، في أي مقاس شاشة أو اتجاه.
- **إحساس تحكّم صحيح**: خطوة فيزياء ثابتة (١٢٠/ث)، و coyote time، و jump buffer، وقفزة متغيّرة الارتفاع.
- **وحدات ثابتة عبر الأجهزة**: الضلع الأقصر = ٤٦٠ وحدة دائماً، والرسم بكثافة بكسل الجهاز.
- **أزرار في مكانها الصحيح**: الحركة يساراً والفعل يميناً — ثابتة فيزيائياً ولا تنعكس مع اتجاه اللغة.

## الصوت والجزيئات

كل المؤثرات مولَّدة رياضياً عبر **Web Audio API**: موجات مربعة ومثلثية وضوضاء مُرشَّحة مع مغلّف ADSR —
قفز، عملة، اصطدام، انفجار، ارتداد، مستوى جديد، نهاية. ومعها موسيقى chiptune بباص وأربيجيو على سلّم خماسي صغير.
ونظام جزيئات بسقف ثابت (٥٢٠) مع حلقات صدمة وأرقام طائرة واهتزاز شاشة واهتزاز الهاتف.

## Gemini — اختياري بالكامل

المحرّك المحلي يقوم بكل ما سبق بلا إنترنت. ربط مفتاح Gemini من **الإعدادات** يضيف نقاشاً مفتوحاً
خارج الصيغ المعروفة وبحثاً حيّاً مع مصادر. المفتاح يُحفظ في `sessionStorage` داخل تبويبك فقط،
ويُمسح بإغلاقه، ولا يُرسل إلا إلى Google مباشرةً.

يستطيع Gemini أيضاً إرسال تعديل مُهيكل على المشروع يُطبَّق تلقائياً:

````
```gm-patch
{"physics":{"gravity":2400},"rules":{"doubleJump":true}}
```
````

## اختصارات

`Enter` إرسال · `Shift+Enter` سطر جديد · `Ctrl+Enter` إعادة بناء اللعبة.
وداخل اللعبة: `الأسهم/WASD` حركة · `مسافة` فعل · `Esc` إيقاف · `M` كتم.

## حدود يجب معرفتها

- التحليل **ثابت (static)**: يقرأ الكود ولا ينفّذه، فقد لا يلتقط قيماً تُحسب وقت التشغيل أو تأتي من مكتبة.
- الأكواد المصغّرة (minified) تُعطي نتائج أضعف لأن أسماء المتغيرات تضيع.
- البحث بلا مفتاح محدود بما تسمح به ويكيبيديا وDuckDuckGo من وصول مباشر من المتصفح.
- الألعاب المولَّدة أشكال هندسية مرسومة بالكود — لا صور ولا ملفات خارجية، وهذا ما يجعلها ملفاً واحداً يعمل بلا إنترنت.

---

<details>
<summary><b>English summary</b></summary>

**GameMorph.AI** is a single-file HTML studio that reverse-engineers HTML5/Canvas games and generates new ones.

Drop in a `.html`/`.js` game and it extracts the real numbers — gravity, jump force, speed, friction, lives —
along with the game loop, delta-time handling, collision detection, scoring, input and audio, then scores the
file against modern mobile-game criteria and tells you what's missing. One click imports that physics into your
project (converting per-frame values to per-second) or rebuilds it as a full mobile version.

Talk to it in plain Arabic and it edits the game for real: genre, physics, palette, names, effects. Drop a `.txt`
brief and it adopts the tone, rules and story. Export gives you one standalone HTML file (~62 KB) with touch
controls, procedurally generated 8-bit Web Audio sound, particles and both orientations — no images, no
libraries, no network requests.

Levels are generated from the player's own physics, so no gap is wider than their jump and no ledge higher than
they can reach, whatever you set the sliders to. Gemini is optional and adds open discussion and live search.

</details>
