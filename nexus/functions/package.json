{
  "name": "nexus-functions",
  "private": true,
  "description": "NEXUS — the API key pool proxy (processRequest)",
  "main": "index.js",
  "engines": { "node": "22" },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth",
    "deploy": "firebase deploy --only functions:processRequest"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.6.0"
  }
}
